# PRD — CleanBallTrio

> 제품 요구사항 문서. 새 기능 / 사용자 흐름 / 수용 기준 추가 시 여기를 먼저 갱신.

## Vision

야구장 다회용기 사용을 게이미피케이션하여 일회용기 사용을 줄인다. 사용자는 다회용기 반납 시 QR을 스캔해 점수를 얻고, 응원 팀별 랭킹에 기여한다.

## Personas (TBD — 사용자 인터뷰 후 보강)

- **P1: 야구 직관러** — 시즌권 보유, 응원팀에 강한 정체성
- **P2: 환경 관심자** — 일회용 줄이기 동기 강함

## Success Metrics (TBD)

- DAU
- QR 스캔 / DAU 비율
- 팀별 평균 누적 점수
- 다회용기 회수율 (구장 운영사 데이터와 매칭 — 별도 협의)

---

## 핵심 기능 (현재 구현 상태)

### F1. 카카오 로그인 ✅ 구현됨
- 카카오 OAuth code → 백엔드 JWT 발급
- 자동 회원가입 (kakao_id를 식별자로)
- 로그아웃 시 카카오 세션도 종료 (logout_redirect_uri로 이동)

**수용 기준**:
- [x] 카카오 로그인 → JWT 받아서 메인 진입
- [ ] 첫 로그인 시 팀 선택 화면 강제 (구현됨? 확인 필요)
- [ ] JWT 만료 처리 (refresh 흐름 — 미구현)

### F2. 팀 선택 + 아바타 (UI 마이그레이션 완료, 백엔드 미구현)
- 사용자가 응원 팀을 1개 선택 (10개 KBO 팀)
- 아바타 커스터마이즈 (의상/모자/액세서리 — `app/avatar.ts`)

**수용 기준**: TBD

### F3. QR 스캔으로 다회용기 사용 인증 (미구현)
- 모바일: 네이티브 카메라 (Expo) → 브릿지로 frontend에 결과 전달
- 웹: html5-qrcode 라이브러리
- 백엔드: QR payload 검증 + 사용 기록 저장 (Cloud SQL)

**수용 기준**: TBD

### F4. 팀별 실시간 랭킹 (미구현)
- Redis(Memorystore) 누적 점수 sorted set
- 실시간 갱신 (WebSocket or polling — TBD)
- 화면: RankingScreen (UI는 마이그레이션됨)

**수용 기준**: TBD

### F5. 사용자 통계 / 등급 (UI 일부, 백엔드 미구현)
- 등급: `app/ecoGrades.ts` 정의됨
- 누적 사용량, 절감 추정치
- 공유용 이미지 생성 (html2canvas) — UI 일부

**수용 기준**: TBD

### F6. 지도 — 다회용기 가능 매장 (UI 마이그레이션됨)
- Kakao Maps JS API
- 마커 클러스터링 (필요 시)

**수용 기준**: TBD

---

## 우선순위 (현재 작업 큐)

이 섹션은 빠르게 변하므로 가벼운 상태 유지:

1. **CI/CD 안정화** — 첫 자동 배포 검증 후 plan 완료
2. **F3 QR 스캔 백엔드** — DB 스키마 + `POST /qr/scan` (별도 plan 필요)
3. **F4 팀 랭킹** — Redis 도입 + 엔드포인트 (별도 plan 필요)
4. **F5 사용자 통계** — F3 데이터 누적 후

세부 plan: `docs/plans/active/`

---

## Out of Scope (현재 단계)

- 결제 / 인앱 구매
- 알림 (push) — 후순위
- 친구/소셜 기능
- 다국어
- iOS/Android 네이티브 (WebView 래핑으로 진행)

---

## Open Questions

- [ ] 다회용기 QR payload 포맷? (구장 운영사와 협의 필요)
- [ ] 점수 산정 알고리즘? (개당 점수 vs 누적 보너스)
- [ ] 등급 임계값? (`ecoGrades.ts` 현재 값 검증 필요)
- [ ] 시즌 리셋 정책? (랭킹을 시즌별로 끊을지)
