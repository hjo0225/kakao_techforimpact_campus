name: Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write
  pull-requests: read

env:
  GCP_PROJECT_ID: cleanballtrio
  GCP_REGION: asia-northeast3
  CLOUD_RUN_SERVICE: cleanballtrio-api
  FIREBASE_PROJECT: cleanballtrio
  PROD_FRONTEND_URL: https://cleanballtrio.web.app

jobs:
  detect-changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      backend: ${{ steps.filter.outputs.backend }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            frontend:
              - 'frontend/**'
              - 'firebase.json'
              - '.firebaserc'
              - '.github/workflows/deploy.yml'
            backend:
              - 'backend/**'
              - '.github/workflows/deploy.yml'

  # 프론트 배포는 deploy-frontend.yml 단일 경로 (키 주입 + SDK 임베드 가드).
  # 이 잡은 CI(타입체크+빌드 검증)만 수행한다 — 여기서 hosting 배포 금지.
  # (과거 사고: 이 잡이 VITE_KAKAO_MAPS_JS_KEY 없이 빌드한 번들로 hosting을 덮어써 지도가 죽음)
  ci-frontend:
    name: Frontend CI
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Build (CI validation)
        env:
          VITE_KAKAO_REST_API_KEY: ${{ secrets.VITE_KAKAO_REST_API_KEY }}
          VITE_KAKAO_MAPS_JS_KEY: ${{ secrets.VITE_KAKAO_MAPS_JS_KEY }}
          VITE_KAKAO_REDIRECT_URI: https://cleanballtrio.web.app/oauth/callback
          VITE_KAKAO_LOGOUT_REDIRECT_URI: https://cleanballtrio.web.app/login
          VITE_API_BASE_URL: https://cleanballtrio-api-1076044788885.asia-northeast3.run.app
        run: npm run build

  ci-backend:
    name: Backend CI + Deploy
    needs: detect-changes
    if: needs.detect-changes.outputs.backend == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Type check
        run: npm run build  # nest build = tsc 컴파일까지 검증

      - name: Authenticate to GCP (WIF)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SA_EMAIL }}

      - name: Setup gcloud
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to Cloud Run (preserves existing env vars)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        working-directory: ${{ github.workspace }}
        run: |
          gcloud run deploy "$CLOUD_RUN_SERVICE" \
            --source backend \
            --region "$GCP_REGION" \
            --platform managed \
            --allow-unauthenticated \
            --quiet
